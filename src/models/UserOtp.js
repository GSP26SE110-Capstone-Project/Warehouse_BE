/**
 * Lưu OTP đăng ký / quên mật khẩu (AuthController).
 * Schema DB: init-scripts/03-complete-schema-with-fk.sql hoặc 05-user-otps.sql
 */
export const tableName = 'user_otps';

export default { tableName };
